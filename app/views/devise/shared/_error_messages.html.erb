<% if resource.errors.any? %>
  <div id="error_explanation" class="msg absolute w-full">
    <ul class="p-4 bg-red-100 text-red-400 font-semibold">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
